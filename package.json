{
  "name": "reps-voting-analysis",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "DEBUG=* node index.js && npm run copy",
    "copy": "cp data/* voting/public/",
    "deploy": "cd voting && npm run build && sed -i 's/=\/js/=js/g' dist/index.html && sed -i 's/=\/css/=css/g' dist/index.html && gh-pages -d dist"
  },
  "dependencies": {
    "async": "^2.5.0",
    "debug": "^3.1.0",
    "dom-storage": "^2.0.2",
    "gh-pages": "^1.2.0",
    "jsdom": "^11.10.0",
    "moment": "^2.22.1",
    "node-fetch": "^1.7.3"
  }
}
